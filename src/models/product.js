class Product {
  constructor(id, name, description, price, image) {
      (this.id = id),
      (this.name = name),
      (this.price = price),
      (this.description = description);
      (this.image = image);
  }
}

export default Product;