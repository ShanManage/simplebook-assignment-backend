class Product {
  constructor(id, name, description, price) {
      (this.id = id),
      (this.name = name),
      (this.price = price),
      (this.description = description);
  }
}

export default Product;